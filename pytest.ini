[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Mark UI tests so they can be run separately
markers =
    ui: marks tests as UI tests (select with '-m ui')

# Configure test discovery
addopts = --strict-markers -v
